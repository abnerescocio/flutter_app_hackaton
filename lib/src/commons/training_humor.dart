import 'package:flutter/cupertino.dart';

class TrainigHumorComponent extends StatelessWidget {
  const TrainigHumorComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
